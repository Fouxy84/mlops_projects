apiVersion: apps/v1
kind: Deployment
metadata:
  name: predict-image-api
  labels:
    app: predict-image-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: predict-image-api
  template:
    metadata:
      labels:
        app: predict-image-api
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: predict-image-api
          image: your-dockerhub-user/mlops-predict-image-api:latest
          ports:
            - containerPort: 8000
          env:
            - name: MODEL_TYPE
              value: cnn
          envFrom:
            - configMapRef:
                name: mlops-config
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "2Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: predict-image-api
  labels:
    app: predict-image-api
spec:
  selector:
    app: predict-image-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
